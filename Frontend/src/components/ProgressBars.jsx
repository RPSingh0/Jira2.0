import {CircularProgress} from "@mui/material";

export function CircularProgressBarSize20() {
    return (
        <CircularProgress
            color="inherit"
            size={20}
        />
    );
}