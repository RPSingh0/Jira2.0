import {Box, styled} from "@mui/material";

export const StyledProjectsPagination = styled(Box)(() => ({
    display: "flex",
    flexDirection: "row",
    justifyContent: "center",
    marginTop: "2rem"
}));